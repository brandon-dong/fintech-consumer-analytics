# Proposal Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete all three proposal deliverables (job posting PDF, proposal PDF, initialized GitHub repo) before April 13, 2026 at 9:55 AM.

**Architecture:** The repo already exists locally with 3 commits (spec, proposal.md, README.md). This plan finishes the setup: `.gitignore`, `CLAUDE.md`, PDF exports, and pushing to a public GitHub repo named `fintech-consumer-analytics`.

**Tech Stack:** Git, GitHub CLI (`gh`), Markdown-to-PDF export (browser print or Pandoc), Bash

---

## File Map

| File | Action | Responsibility |
|---|---|---|
| `.gitignore` | Create | Prevent committing secrets, caches, venvs |
| `CLAUDE.md` | Create | Project context for Claude Code + KB query conventions |
| `docs/job-posting.pdf` | Create | Copy of the Indeed job posting PDF |
| `docs/proposal.pdf` | Create | Exported PDF of docs/proposal.md |
| `docs/proposal.md` | Modify | Add GitHub repo URL after push |
| `.env.example` | Create | Template for required environment variables |

---

## Task 1: Create .gitignore

**Files:**
- Create: `.gitignore`

- [ ] **Step 1: Write .gitignore**

Create `.gitignore` at the repo root with this exact content:

```
# Environment & secrets
.env
*.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.pyc
*.pyo
*.pyd
.Python
venv/
env/
.venv/
*.egg-info/
dist/
build/
.eggs/

# dbt
target/
dbt_packages/
logs/
profiles.yml

# Snowflake
.snowflake/

# OS
.DS_Store
Thumbs.db

# IDE
.cursor/
.vscode/
.idea/
*.swp

# Jupyter
.ipynb_checkpoints/

# Streamlit
.streamlit/secrets.toml
```

- [ ] **Step 2: Verify .gitignore works**

Create a test file then verify git ignores it:

```bash
echo "SNOWFLAKE_PASSWORD=secret" > .env
git status
```

Expected output: `.env` should NOT appear in untracked files. If it does, the .gitignore isn't working — check the file was saved to the repo root.

```bash
rm .env
```

- [ ] **Step 3: Commit**

```bash
git add .gitignore
git commit -m "Add .gitignore for secrets, Python, dbt, and Snowflake"
```

Expected: `[main xxxxxxx] Add .gitignore for secrets, Python, dbt, and Snowflake`

---

## Task 2: Create .env.example

**Files:**
- Create: `.env.example`

- [ ] **Step 1: Write .env.example**

```
# Snowflake credentials — copy to .env and fill in values
# Never commit .env to git
SNOWFLAKE_ACCOUNT=
SNOWFLAKE_USER=
SNOWFLAKE_PASSWORD=
SNOWFLAKE_DATABASE=FINTECH_ANALYTICS
SNOWFLAKE_SCHEMA=RAW
SNOWFLAKE_WAREHOUSE=COMPUTE_WH
SNOWFLAKE_ROLE=

# CFPB API (no key required — public API)
CFPB_API_BASE_URL=https://www.consumerfinance.gov/data-research/consumer-complaints/search/api/v1/
```

- [ ] **Step 2: Verify .env.example is tracked**

```bash
git status
```

Expected: `.env.example` appears in untracked files (it should be tracked, unlike `.env`).

- [ ] **Step 3: Commit**

```bash
git add .env.example
git commit -m "Add .env.example with required environment variables"
```

---

## Task 3: Create CLAUDE.md

**Files:**
- Create: `CLAUDE.md`

- [ ] **Step 1: Write CLAUDE.md**

```markdown
# CLAUDE.md — Fintech Consumer Analytics

## Project Overview

End-to-end analytics pipeline on the CFPB Consumer Complaint Database.
Analyzes complaint trends across fintech products and companies to surface
consumer trust signals. Built to demonstrate Marketing Science analyst skills
targeting the Greenlight Financial Technology Associate Data Analyst role.

**Central business question:** Which fintech products and companies are losing
consumer trust, and why?

## Tech Stack

- **Extraction:** Python (cfpb_extract.py) via CFPB REST API
- **Data Warehouse:** Snowflake (AWS US East 1)
- **Transformation:** dbt Core
- **Orchestration:** GitHub Actions (scheduled daily, 6 AM UTC)
- **Dashboard:** Streamlit (deployed to Streamlit Community Cloud)
- **Knowledge Base:** Claude Code (scrape → summarize → query)

## Snowflake Schema Structure

```
Database: FINTECH_ANALYTICS
├── RAW           ← append-only raw loads, never modified after insert
│   └── CFPB_COMPLAINTS
├── STAGING       ← dbt staging models (clean, renamed, type-cast)
│   └── STG_CFPB_COMPLAINTS
└── MART          ← dbt mart models (star schema)
    ├── FACT_COMPLAINTS
    ├── DIM_PRODUCT
    ├── DIM_COMPANY
    ├── DIM_ISSUE
    ├── DIM_GEOGRAPHY
    └── DIM_DATE
```

## Directory Structure

```
fintech-consumer-analytics/
├── .github/workflows/
│   ├── extract_cfpb.yml       # daily CFPB extraction to Snowflake
│   └── scrape_knowledge.yml   # knowledge base scraping
├── extract/
│   └── cfpb_extract.py        # CFPB API → Snowflake RAW
├── dbt_project/
│   ├── models/
│   │   ├── staging/           # stg_cfpb_complaints.sql
│   │   └── mart/              # fact_complaints.sql, dim_*.sql
│   ├── tests/                 # dbt schema tests
│   └── dbt_project.yml
├── dashboard/
│   └── app.py                 # Streamlit dashboard
├── knowledge/
│   ├── raw/                   # scraped sources (15+ files)
│   └── wiki/                  # Claude Code-generated wiki pages
│       ├── index.md
│       ├── overview.md
│       ├── key-entities.md
│       └── complaint-themes.md
├── docs/
│   ├── job-posting.pdf
│   ├── proposal.pdf
│   └── superpowers/
│       ├── specs/             # design documents
│       └── plans/             # implementation plans
├── .env.example
├── .gitignore
├── CLAUDE.md                  # this file
└── README.md
```

## Environment Variables

All credentials are in `.env` (gitignored). Copy `.env.example` to `.env`.
GitHub Actions uses repository secrets with the same variable names.

Never commit `.env`, `profiles.yml`, or any file containing credentials.

## dbt Conventions

- Staging models: `stg_<source>_<entity>.sql` — one model per source table
- Mart models: `fact_<entity>.sql`, `dim_<entity>.sql`
- All mart models materialized as `table` in Snowflake
- At least one schema test per model (not_null on PKs, unique on natural keys)
- Run with: `cd dbt_project && dbt run && dbt test`

## Knowledge Base — Query Conventions

When asked to answer questions about the fintech knowledge base:

1. Read `knowledge/index.md` first to understand what's available
2. Read the relevant wiki page(s) in `knowledge/wiki/`
3. Fall back to specific files in `knowledge/raw/` for source-level detail
4. Synthesize across sources — do not just summarize one file
5. Cite your sources (filename) when making specific claims

**Example queries:**
- "What does my knowledge base say about why mobile banking apps generate consumer disputes?"
- "Which fintech companies have the worst complaint resolution track record?"
- "What regulatory trends from CFPB reports should a family fintech company watch?"

## Star Schema — Key Fields

`fact_complaints` analytic measures:
- `timely_response_flag` — did the company respond on time? (Boolean)
- `consumer_disputed_flag` — did the consumer dispute the resolution? (Boolean)
- `submitted_via` — channel (web, phone, referral, fax)

These three fields are the core of all dashboard analytics. High `consumer_disputed_flag`
rate = poor company response quality. Low `timely_response_flag` rate = operational issues.
```

- [ ] **Step 2: Verify CLAUDE.md renders**

Open `CLAUDE.md` in your editor and confirm:
- The nested code block (Snowflake schema tree) renders correctly
- No broken markdown
- All directory paths match what's actually in the repo

- [ ] **Step 3: Commit**

```bash
git add CLAUDE.md
git commit -m "Add CLAUDE.md with project context and knowledge base query conventions"
```

---

## Task 4: Push to GitHub

**Files:** (no file changes — GitHub remote setup)

- [ ] **Step 1: Create public GitHub repo**

```bash
gh repo create fintech-consumer-analytics --public --source=. --remote=origin --push
```

Expected output:
```
✓ Created repository <your-username>/fintech-consumer-analytics on GitHub
✓ Added remote https://github.com/<your-username>/fintech-consumer-analytics.git
✓ Pushed commits to https://github.com/<your-username>/fintech-consumer-analytics.git
```

If `gh` is not authenticated, run `gh auth login` first and follow the prompts.

- [ ] **Step 2: Verify repo is public**

```bash
gh repo view fintech-consumer-analytics --json visibility -q .visibility
```

Expected: `PUBLIC`

- [ ] **Step 3: Copy your repo URL**

```bash
gh repo view fintech-consumer-analytics --json url -q .url
```

Copy the output (e.g., `https://github.com/brandondong/fintech-consumer-analytics`). You'll need it in the next task.

---

## Task 5: Update proposal.md with Repo URL

**Files:**
- Modify: `docs/proposal.md` line 7

- [ ] **Step 1: Edit proposal.md**

Open `docs/proposal.md` and replace:

```
**GitHub Repo:** [Add public repo URL after initializing on GitHub]
```

With your actual repo URL (from Task 4 Step 3):

```
**GitHub Repo:** https://github.com/<your-username>/fintech-consumer-analytics
```

- [ ] **Step 2: Also review the reflection paragraph**

Read the reflection paragraph out loud. Edit any phrasing that doesn't sound like your natural voice. The content should stay the same — just make sure it reads as something you wrote, not something AI-generated.

- [ ] **Step 3: Commit**

```bash
git add docs/proposal.md
git commit -m "Add GitHub repo URL to proposal"
```

- [ ] **Step 4: Push**

```bash
git push
```

---

## Task 6: Add Job Posting PDF

**Files:**
- Create: `docs/job-posting.pdf`

- [ ] **Step 1: Copy the PDF**

The job posting PDF is already in the repo root as:
`Associate Data Analyst Atlanta (Remote Friendly) - Atlanta, GA - Indeed.com.pdf`

Copy it to the required location:

```bash
cp "Associate Data Analyst Atlanta (Remote Friendly) - Atlanta, GA - Indeed.com.pdf" docs/job-posting.pdf
```

- [ ] **Step 2: Verify**

```bash
ls docs/
```

Expected: `job-posting.pdf` appears in the listing.

- [ ] **Step 3: Commit and push**

```bash
git add docs/job-posting.pdf
git commit -m "Add job posting PDF to docs/"
git push
```

---

## Task 7: Export Proposal to PDF

**Files:**
- Create: `docs/proposal.pdf`

This is a manual step — export `docs/proposal.md` to PDF using one of these methods:

- [ ] **Option A (Recommended — browser print):**

  1. Open `docs/proposal.md` in VS Code or Cursor
  2. Use the Markdown Preview panel (Ctrl+Shift+V)
  3. Right-click the preview → Print → Save as PDF
  4. Save to `docs/proposal.pdf`

- [ ] **Option B (Pandoc, if installed):**

  ```bash
  pandoc docs/proposal.md -o docs/proposal.pdf
  ```

  If Pandoc is not installed: `winget install JohnMacFarlane.Pandoc`

- [ ] **Step: Verify the PDF looks clean**

Open `docs/proposal.pdf` and confirm:
- Name, project name, and repo URL are correct
- SQL requirement quote is present
- Reflection paragraph is one clean paragraph (4-6 sentences)
- No markdown syntax visible (no `**`, `##`, `[]`)

- [ ] **Step: Commit and push**

```bash
git add docs/proposal.pdf
git commit -m "Add proposal PDF for submission"
git push
```

---

## Task 8: Final Verification

- [ ] **Step 1: Check repo structure**

```bash
ls docs/
```

Expected output includes: `job-posting.pdf`, `proposal.md`, `proposal.pdf`

```bash
ls
```

Expected output includes: `.gitignore`, `.env.example`, `CLAUDE.md`, `README.md`

- [ ] **Step 2: Verify no secrets are committed**

```bash
git log --oneline
git show HEAD --stat
```

Confirm no `.env` files appear in any commit.

- [ ] **Step 3: Verify repo is public and complete on GitHub**

Open your GitHub repo URL in a browser. Confirm:
- Repo is public
- README.md renders on the homepage
- `docs/` folder contains both PDFs
- `CLAUDE.md` is present

- [ ] **Step 4: Submit to Brightspace**

Submit your GitHub repo URL to Brightspace before April 13 at 9:55 AM.

---

## What Comes Next

This plan covers the **Proposal (Apr 13)**. Two more plans to write when those phases begin:

**Milestone 01 Plan** (write around Apr 14, due Apr 27):
- CFPB extraction script (`extract/cfpb_extract.py`)
- Snowflake raw table setup
- dbt project init + staging model (`stg_cfpb_complaints.sql`)
- dbt mart models (star schema: `fact_complaints`, `dim_*`)
- GitHub Actions workflow (`extract_cfpb.yml`)
- Pipeline diagram in README

**Milestone 02 Plan** (write around Apr 28, due May 4):
- Knowledge base scraping + GitHub Actions workflow
- Streamlit dashboard (`dashboard/app.py`) deployed to Community Cloud
- Wiki page generation with Claude Code
- Presentation slides (PDF)
- README polish (pipeline diagram, ERD, dashboard screenshot, key insights)
- ERD generated from dbt models
